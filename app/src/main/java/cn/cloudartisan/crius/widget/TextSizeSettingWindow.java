/**
  * Generated by smali2java 1.0.0.558
  * Copyright (C) 2013 Hensence.com
  */

package cn.cloudartisan.crius.widget;

import android.widget.PopupWindow;
import android.view.View;
import android.widget.CheckBox;
import android.content.Context;
import android.view.LayoutInflater;
import android.graphics.drawable.ColorDrawable;
import cn.cloudartisan.crius.R;

public class TextSizeSettingWindow extends PopupWindow implements View.OnClickListener {
    CheckBox checkBox1;
    CheckBox checkBox2;
    CheckBox checkBox3;
    CheckBox checkBox4;
    TextSizeSettingWindow.OnSizeSelectedListener clickListener;
    Context mContext;
    
    public TextSizeSettingWindow(Context context, TextSizeSettingWindow.OnSizeSelectedListener listener) {
        super(context, null);
        mContext = context;
        clickListener = listener;
        setContentView(LayoutInflater.from(context).inflate(R.layout.layout_text_size_setting, null));
        setWidth(-0x1);
        setHeight((context.getResources().getDimensionPixelOffset(R.dimen.padding_10dip) * 0x5));
        setOutsideTouchable(true);
        setFocusable(true);
        setBackgroundDrawable(new ColorDrawable(0x0));
        checkBox1 = (CheckBox)getContentView().findViewById(R.id.radioButton1);
        checkBox2 = (CheckBox)getContentView().findViewById(R.id.radioButton2);
        checkBox3 = (CheckBox)getContentView().findViewById(R.id.radioButton3);
        checkBox4 = (CheckBox)getContentView().findViewById(R.id.radioButton4);
        getContentView().findViewById(R.id.panel1).setOnClickListener(this);
        getContentView().findViewById(R.id.panel2).setOnClickListener(this);
        getContentView().findViewById(R.id.panel3).setOnClickListener(this);
        getContentView().findViewById(R.id.panel4).setOnClickListener(this);
        setAnimationStyle(R.style.Animations_PopUpMenu_BottomToTop);
    }
    
    public void onClick(View v) {
        if((v.getId() == R.id.panel1) && (!checkBox1.isChecked())) {
            checkBox2.setChecked(false);
            checkBox3.setChecked(false);
            checkBox4.setChecked(false);
            checkBox1.setChecked(true);
            clickListener.onSizeSelected(0x50);
        }
        if((v.getId() == R.id.panel2) && (!checkBox2.isChecked())) {
            checkBox1.setChecked(false);
            checkBox3.setChecked(false);
            checkBox4.setChecked(false);
            checkBox2.setChecked(true);
            clickListener.onSizeSelected(0x64);
        }
        if((v.getId() == R.id.panel3) && (!checkBox3.isChecked())) {
            checkBox2.setChecked(false);
            checkBox1.setChecked(false);
            checkBox4.setChecked(false);
            checkBox3.setChecked(true);
            clickListener.onSizeSelected(0x78);
        }
        if((v.getId() == R.id.panel4) && (!checkBox4.isChecked())) {
            checkBox2.setChecked(false);
            checkBox3.setChecked(false);
            checkBox1.setChecked(false);
            checkBox4.setChecked(true);
            clickListener.onSizeSelected(0x96);
        }
    }
    public  interface OnSizeSelectedListener
    {
        void onSizeSelected(int paramInt);
    }
}
